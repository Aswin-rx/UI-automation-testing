{
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.15.19"
  },
  "scripts": {
    "test": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "playwright": {
    "timeout": 30000,
    "retries": 0,
    "reporter": "list"
  }
}
